name: flutter_qwen_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  provider: ^6.1.3
  path_provider: ^2.0.14
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true  
    